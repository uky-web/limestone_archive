{% set props = {
    'links': [
        {'label': fake('word')|titleize,'url':'#' },
        {'label': fake('word')|titleize,'url':'#' },
        {'label': fake('word')|titleize,'url':'#' }
    ]
}%}

{% include '@limestone/utility-nav.twig' with props only %}
