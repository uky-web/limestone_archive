<dl class="described-links">
  {% for i in 1..3 %}
    <dt>{% include '@limestone/link.twig' with { label:fake('words',{nb:4, asText: true}), 'link_variant':'link--fancy' } only %} </dt> 
    {% if not brief %}
    <dd>{% include 'atoms-formatted-text' with {formatted_text_sentences: 2} %}</dd>
    {% endif %}
  {% endfor %}
</dl>
