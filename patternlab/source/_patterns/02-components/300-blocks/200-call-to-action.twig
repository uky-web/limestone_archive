{% set body %}
    {% include "@limestone/formatted-text.twig" with {content: text.editorial_paragraphs(3, 5)} only %}
{% endset %}

{% set cta_props = {
    "cta_variant": cta_variant,
    "headline": headline|default(fake('bs')|titleize),
    "headline_level":  level|default(2) ,
    "body": body,
    "button_label": 'Call To Action text',
    "button_url": "example.com",
}%}

{% include "@limestone/call-to-action.twig" with cta_props only %}
