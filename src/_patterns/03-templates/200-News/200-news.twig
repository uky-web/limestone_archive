{% extends 'base.twig' %}

{% block main_content %}
    {% include 'components-' ~ page_header|default('hero-page-header') with { head: "Exciting news story headline", slab_variant : header_slab|default('slab--wildcat-white') } %}
    {% include 'components-content-meta-slab' with { slab_variant : slab_variant|default('slab--wildcat-white') } %}
    {% include 'components-sidebar-editorial' with { slab_variant : slab_variant|default('slab--wildcat-white') }  %} 
{% endblock %}

