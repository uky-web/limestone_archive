{% set horizontal = fake('imageUrl',{width: 1600, height: 700}) %}
{% set vertical = fake('imageUrl',{width: 750, height: 1334}) %}


<picture>
	<source srcset="{{ horizontal }}" media="(min-width: 58rem)">
	<source srcset="{{ vertical }}">
	{% include 'atoms-image' %}
</picture>