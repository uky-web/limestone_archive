{% set horizontal = fake('imageUrl',{width: 1600, height: 700}) %}
{% set vertical = fake('imageUrl',{width: 550, height: 734}) %}


<picture>
	<source srcset="{{ horizontal }}" media="(min-width: 35rem)">
	<source srcset="{{ vertical }}">
	{% include 'atoms-image' %}
</picture>