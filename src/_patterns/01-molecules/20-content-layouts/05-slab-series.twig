{# <div class="slab {{ slab_variant }}" {{ props|join(' ') }} {{ attrs|attr_list }}>
    {% block slab_outer %}
        <div class="slab__wrapper">
            {% block slab_content %}
                {% include 'atoms-placeholder' %}
            {% endblock %}
        </div>
    {% endblock %}
</div> #}


<div class="slab-series {{slab_series_variant}}">
        {% include 'components-' ~ page_header|default('overlay-page-header') with { 
            with_cta : true,
            with_editorial:true,
            cta_type : "mixed",
            super : "Overlay Hero Header",
            id: "item-1",
            slab_variant : header_slab|default('slab--wildcat-white'),
            with_cta: true,
            with_next: "Item 2",
            next_id: "item-2"
        } 
    %}
        {% include 'components-' ~ page_header|default('overlay-page-header') with { 
            with_cta : true,
            with_editorial:true,
            cta_type : "mixed",
            super : "Overlay Hero Header",
            id: "item-2",
            slab_variant : header_slab|default('slab--wildcat-blue'),
            with_cta: true,
            with_next: "Item 3",
            next_id: "item-3"
        } 
    %}
        {% include 'components-' ~ page_header|default('overlay-page-header') with { 
            with_cta : true,
            with_editorial:true,
            cta_type : "mixed",
            super : "Overlay Hero Header",
            id: 'item-3',
            slab_variant : header_slab|default('slab--wildcat-white'),
            with_cta: false,
            with_next: "Item 1",
            next_id: "item-1"
        } 
    %}
</div>