{% set icon_button_data_attributes = {
    'toggle':toggle,
    'toggle_viz':true,
} %}

{% set icon_button_props = {
    'button_variant': toggle_button_variant ~ " toggle-button", 
    'icon_label_variant':icon_label_variant,
    'icon':icon,
    'icon_label':label,
    'toggle_class': toggle_class,
    'data_attributes': icon_button_data_attributes|merge(data_attributes|default([])),
    'html_attributes': { aria_pressed: "false"}|merge(html_attributes|default([]))
}%}

{% include '@limestone/icon-button.twig' with icon_button_props only %}
